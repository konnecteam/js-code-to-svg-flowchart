const code = `
(models, currentModel, currentUser, options, t) => {
    return xxxx.yyyy.getZ(models, t)
        .then(ok => {
        const xxxyyyyzzzz = 5 + ok;
        return xxxyyyyzzzz;
    });
}
`;
